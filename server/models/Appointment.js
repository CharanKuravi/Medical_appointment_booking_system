// SQLite-based Appointment model
// No Mongoose needed - using direct SQLite queries

module.exports = {
    // Appointment model is handled directly in database/db.js
    // All queries are done through the db connection
};
